import React from 'react'

//In the future, will be a navbar that allows user to select tabs, sign in, sign up, log out, etc...

const Header = () => {
    return (
        <div>
            <h1>  This is the header </h1>
        </div>
    )
}

export default Header;